<%= form_for(@project_revenue_event) do |f| %>
  <% if @project_revenue_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_revenue_event.errors.count, "error") %> prohibited this project_revenue_event from being saved:</h2>

      <ul>
      <% @project_revenue_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
