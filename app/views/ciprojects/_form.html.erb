<%= form_for(@ciproject) do |f| %>
  <% if @ciproject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ciproject.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @ciproject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
